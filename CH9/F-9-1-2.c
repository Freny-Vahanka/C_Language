#include<stdio.h>

/* 1.WAP to convert given character in uppercase.*/

void main(){
	
		char a;
		
		printf("Enter any character in lower case to convert into UPPERCASE: ");
		scanf("%s",&a);
		
		printf("UPPER CASE : %c",a-32);
}

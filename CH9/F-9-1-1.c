#include<stdio.h>

/* 2.WAP to convert given character in lowercase.*/

void main(){
	
		char a;
		
		printf("Enter any character in UPPERCASE to convert into lowercase: ");
		scanf("%s",&a);
		
		printf("lower case : %c",a+32);
}
